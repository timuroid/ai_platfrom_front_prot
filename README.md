# UralSteel UI · LLM Assistant

Современное веб-приложение для работы с LLM-ассистентом, построенное на React и Vite.

## 📋 Требования

Перед началом работы убедитесь, что на вашем компьютере установлены:

- **Node.js** версии 18 или выше
- **npm** (обычно устанавливается вместе с Node.js) или **yarn**

### Проверка установки

Откройте терминал (командную строку) и выполните:

```bash
node --version
npm --version
```

Если команды выводят версии, значит всё установлено правильно.

## 🚀 Быстрый старт

### Вариант 1: Клонирование существующего репозитория

Если проект уже находится в Git-репозитории на GitHub:

```bash
git clone <url-репозитория>
cd minimal_front
```

### Вариант 2: Загрузка проекта на GitHub (с нуля)

Если вы только что создали репозиторий на GitHub и хотите загрузить проект:

1. **Инициализация Git в проекте** (если еще не сделано):
   ```bash
   cd minimal_front
   git init
   ```

2. **Добавление всех файлов в Git**:
   ```bash
   git add .
   ```

3. **Создание первого коммита**:
   ```bash
   git commit -m "Initial commit"
   ```

4. **Добавление удаленного репозитория** (замените `<ваш-username>` и `<название-репозитория>`):
   ```bash
   git remote add origin https://github.com/<ваш-username>/<название-репозитория>.git
   ```
   
   Или если используете SSH:
   ```bash
   git remote add origin git@github.com:<ваш-username>/<название-репозитория>.git
   ```

5. **Загрузка кода на GitHub**:
   ```bash
   git branch -M main
   git push -u origin main
   ```

> 💡 **Примечание:** Если у вас возникли проблемы с аутентификацией, используйте [Personal Access Token](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token) вместо пароля.

### Установка зависимостей

Установите все необходимые пакеты проекта:

```bash
npm install
```

Эта команда установит все зависимости, указанные в `package.json`. Процесс может занять несколько минут.

### Запуск проекта в режиме разработки

После успешной установки зависимостей запустите сервер разработки:

```bash
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

Откройте этот адрес в браузере, и вы увидите работающее приложение.

> 💡 **Примечание:** Сервер разработки автоматически перезагружает страницу при изменении файлов (Hot Module Replacement).

## 📜 Доступные команды

### `npm run dev`
Запускает сервер разработки с горячей перезагрузкой. Приложение будет доступно на `http://localhost:5173`

### `npm run build`
Создаёт оптимизированную production-сборку проекта в папку `dist/`

### `npm run preview`
Предварительный просмотр production-сборки (требует предварительного выполнения `npm run build`)

### `npm run lint`
Проверяет код на наличие ошибок и предупреждений с помощью ESLint

## 🛠 Технологии

Проект использует следующие технологии:

- **React 19** - библиотека для создания пользовательских интерфейсов
- **Vite** - быстрый сборщик и dev-сервер
- **Tailwind CSS** - утилитарный CSS-фреймворк
- **Lucide React** - библиотека иконок
- **ESLint** - линтер для проверки качества кода

## 📁 Структура проекта

```
minimal_front/
├── public/              # Статические файлы
│   ├── vite.svg
│   └── uralsteel-design-tokens.v1.2.css
├── src/                 # Исходный код приложения
│   ├── admin/          # Компоненты админ-панели
│   │   ├── Admin.css
│   │   └── AdminDashboard.jsx
│   ├── components/     # Переиспользуемые компоненты
│   │   ├── Sidebar.jsx
│   │   ├── TeachingCards.jsx
│   │   └── TeachingCards.css
│   ├── App.jsx         # Главный компонент приложения
│   ├── App.css         # Стили приложения
│   ├── index.css       # Глобальные стили
│   └── main.jsx        # Точка входа приложения
├── index.html          # HTML-шаблон
├── package.json        # Зависимости и скрипты проекта
├── vite.config.js     # Конфигурация Vite
├── tailwind.config.js  # Конфигурация Tailwind CSS
└── postcss.config.js   # Конфигурация PostCSS
```

## 🎨 Особенности

- **Адаптивный дизайн** - приложение корректно работает на всех устройствах
- **Темная тема** - поддержка темной темы оформления
- **Админ-панель** - раздел для администраторов с расширенным функционалом
- **Боковая панель** - навигация с возможностью сворачивания
- **Современный UI** - использование дизайн-токенов UralSteel

## 🔧 Решение проблем

### Порт 5173 уже занят

Если порт 5173 занят другим процессом, Vite автоматически предложит использовать другой порт. Или вы можете указать порт вручную:

```bash
npm run dev -- --port 3000
```

### Ошибки при установке зависимостей

Если возникают проблемы при `npm install`:

1. Удалите папку `node_modules` и файл `package-lock.json`:
   ```bash
   rm -rf node_modules package-lock.json
   ```
   (На Windows: `rmdir /s node_modules` и `del package-lock.json`)

2. Очистите кэш npm:
   ```bash
   npm cache clean --force
   ```

3. Установите зависимости заново:
   ```bash
   npm install
   ```

### Проблемы с правами доступа

На Linux/Mac может потребоваться использовать `sudo` для некоторых команд. Однако лучше настроить npm для работы без sudo:

```bash
mkdir ~/.npm-global
npm config set prefix '~/.npm-global'
```

## 🔄 Работа с Git и GitHub

### Основные команды Git

После внесения изменений в проект:

1. **Проверка изменений**:
   ```bash
   git status
   ```

2. **Добавление измененных файлов**:
   ```bash
   git add .
   ```
   Или для конкретного файла:
   ```bash
   git add путь/к/файлу
   ```

3. **Создание коммита**:
   ```bash
   git commit -m "Описание изменений"
   ```

4. **Загрузка изменений на GitHub**:
   ```bash
   git push
   ```

### Получение последних изменений с GitHub

Если вы работаете на нескольких компьютерах или в команде:

```bash
git pull
```

### Создание новой ветки

Для работы над новой функцией:

```bash
git checkout -b название-новой-ветки
git push -u origin название-новой-ветки
```

### Полезные команды

- `git log` - просмотр истории коммитов
- `git diff` - просмотр изменений перед коммитом
- `git branch` - список всех веток
- `git checkout main` - переключение на основную ветку

## 📝 Дополнительная информация

- Документация Vite: https://vite.dev
- Документация React: https://react.dev
- Документация Tailwind CSS: https://tailwindcss.com
- Документация Git: https://git-scm.com/doc
- Документация GitHub: https://docs.github.com

## 📄 Лицензия

Этот проект является приватным.

---

**Удачной разработки! 🚀**
